- Delete namecoin file from imports/api/namecoin
- Doi blockchain update // Not an update anymore but an insert (See 'Soi to blockchain')
- GUI
  - Start page content
  - Recipients page: Add id
  - Add senders page
  - Scrollable table:
    - useless in mobile view
    - header need overflow hidden css
- Readme
- Tests
! Remove admin account creaton on first start
! Delete rest test route
? Token registration
? addOptIn data_json valdidation
? Check if rest api is thread-safe
- Soi to blockchain  // Not anymore because we need the doi confirmation IP for the e/hash generation (in consultation with tobias)
